# 開発履歴

## セッション情報

- **開発日**: 2026-01-13
- **開発者**: SAMESHIMA-Masahiro + Claude Sonnet 4.5
- **使用モデル**: Claude Sonnet 4.5 (claude-sonnet-4-5-20250929)
- **開発環境**: Claude Code CLI

## 開発フロー

### 1. プロジェクト初期化

#### 初期ゲーム作成
- 1匹の鮫の基本実装
- Canvas描画システム
- 餌やり機能
- 満腹度システム
- アニメーション（尻尾、瞬き）

#### リポジトリセットアップ
- GitHubリポジトリ作成: `tiburoncitos`
- GitHub Pages設定
- 初回コミット

**コミット**: `Initial commit: コザメ🦈 - かわいい鮫の癒しゲーム`

---

### 2. 複数の鮫と性格システム

#### 実装内容
- 3匹の鮫を追加
- 性格システム導入:
  - おっちょこちょい（clumsy）
  - よくばり（greedy）
  - おくびょう（shy）
- 各鮫に異なる色を設定
- 性格に応じた行動ロジック

**コミット**: `Add 3 sharks with unique personalities`

**主要変更点**:
```javascript
// 性格別の色分け
getColors() {
  switch(this.personality) {
    case 'clumsy': return { body: '#FFB6C1', ... }
    case 'greedy': return { body: '#FFA07A', ... }
    case 'shy': return { body: '#ADD8E6', ... }
  }
}
```

---

### 3. 餌のバリエーション追加

#### 実装内容
- 3種類の餌を実装:
  - 通常の餌（オレンジ）
  - ゆっくり沈む餌（青緑）
  - 浮かぶ餌（クリーム）
- 餌タイプごとの物理挙動
- 色分けと視覚的な区別

**コミット**: `Add food type preferences for sharks`

**主要変更点**:
```javascript
constructor(x, y, type) {
  this.type = type || 'normal';
  switch(this.type) {
    case 'slow': this.gravity = 0.08; break;
    case 'floating': this.gravity = -0.15; break;
    default: this.gravity = 0.3; break;
  }
}
```

---

### 4. 餌と性格の相性システム

#### 実装内容
- 臆病な鮫がゆっくり沈む餌を優先
- おっちょこちょいが浮かぶ餌を優先
- 好みの餌に対して速度2.2倍で反応
- 優先度判定システム

**コミット**: `Add food type preferences for sharks`

**主要ロジック**:
```javascript
// 好みの餌をチェック
if (this.personality === 'shy' && food.type === 'slow') {
  isPreferred = true;
}
if (preferredFood && preferredDist < preferredRange) {
  this.speed = this.baseSpeed * 2.2;
}
```

---

### 5. 浮かぶ餌の修正

#### 問題
- 浮かぶ餌が出現しない（重力が弱すぎた）

#### 修正内容
- 重力を -0.05 → -0.15 に強化
- 最大速度を 0.8 → 2.5 に増加
- 初期速度 -1 を設定

**コミット**: `Fix floating food not appearing issue`

---

### 6. リセット機能追加

#### 実装内容
- 画面右上にリセットボタン配置
- `initGame()` 関数でゲーム状態を初期化
- レスポンシブ対応のボタンスタイル
- ホバー/クリックアニメーション

**コミット**: `Add reset button to restart game`

**初期化する要素**:
- 鮫の位置
- 餌と泡の配列
- 満腹度（100%）
- 餌カウント（0）

---

### 7. 満腹システム実装

#### 実装内容
- 30秒以内に20個の餌を食べると満腹状態に
- 満腹時の効果:
  - 1.5倍に太る
  - 動けなくなる
  - 60秒後に自動回復
- 視覚的フィードバック:
  - Zzzマーク
  - 眠っている口
  - パーティクルエフェクト

**コミット**: `Add overeating system for sharks`

**主要ロジック**:
```javascript
// 30秒以内の餌を記録
this.recentEats.push(Date.now());
this.recentEats = this.recentEats.filter(
  time => Date.now() - time < 30000
);

// 20個以上で満腹
if (this.recentEats.length >= 20) {
  this.isStuffed = true;
  this.stuffedTimer = 3600; // 60秒
}
```

---

## コミット履歴まとめ

| コミット | 内容 | 日時 |
|---------|------|------|
| dd9c91f | Update README with GitHub Pages URL | 2026-01-13 |
| c75e257 | Initial commit: コザメ🦈 | 2026-01-13 |
| b09062e | Add 3 sharks with unique personalities | 2026-01-13 |
| 77600d7 | Add food type preferences for sharks | 2026-01-13 |
| 3bcccb0 | Fix floating food not appearing issue | 2026-01-13 |
| 8777706 | Add reset button to restart game | 2026-01-13 |
| 6837a9d | Add overeating system for sharks | 2026-01-13 |

## 主要な技術的決定

### 1. Canvas vs DOM
- **決定**: Canvas使用
- **理由**:
  - 滑らかなアニメーション
  - 複数要素の高速描画
  - パーティクルエフェクトの実装が容易

### 2. クラスベース設計
- `Shark` クラス: 鮫の状態と動作
- `Food` クラス: 餌の種類と物理挙動
- `Particle` クラス: エフェクト
- `Bubble` クラス: 背景演出

### 3. 状態管理
- グローバル配列で管理: `sharks[]`, `foods[]`, `particles[]`, `bubbles[]`
- 各オブジェクトが自己完結的に状態を管理

### 4. レスポンシブ対応
- Canvas内部サイズは固定（800x600）
- CSS で表示サイズを調整
- 座標変換関数で正確なクリック判定

## 開発時の課題と解決策

### 課題1: 浮かぶ餌が見えない
- **原因**: 重力が弱すぎて浮上速度が遅い
- **解決**: 重力と初期速度を調整

### 課題2: 満腹判定のタイミング
- **原因**: フレームベースだと不正確
- **解決**: `Date.now()` でミリ秒単位の時刻を記録

### 課題3: レスポンシブでのクリック座標
- **原因**: Canvas表示サイズと内部サイズの差異
- **解決**: スケール変換関数を実装

## パフォーマンス最適化

1. **配列の逆順ループ**: 削除時の効率化
```javascript
for (let i = foods.length - 1; i >= 0; i--) {
  if (condition) foods.splice(i, 1);
}
```

2. **早期リターン**: 満腹時の処理スキップ
```javascript
if (this.isStuffed) return;
```

3. **フィルタリング**: 古いタイムスタンプの削除
```javascript
this.recentEats = this.recentEats.filter(
  time => currentTime - time < 30000
);
```

## 使用したツール

- **Claude Code CLI**: 統合開発環境
- **Git**: バージョン管理
- **GitHub CLI (`gh`)**: リポジトリ管理
- **GitHub Pages**: デプロイ

## 開発メトリクス

- **総コミット数**: 7
- **開発時間**: 約3時間
- **ファイル数**: 4（HTML 2, Markdown 2）
- **コード行数**: 約850行（index.html）
